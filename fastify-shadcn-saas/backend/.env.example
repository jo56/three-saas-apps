DATABASE_URL="postgresql://saasuser:saaspass@localhost:5432/saasdb?schema=public"

# JWT Secret for User Authentication (PRODUCTION ONLY)
# Used to sign and verify JWT tokens for user authentication
# REQUIRED when enabling authentication middleware (see src/index.ts and src/middleware/auth.ts)
#
# Generate a secure secret with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# JWT_SECRET="your-secure-random-secret-here-min-32-characters"
