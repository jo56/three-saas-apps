# Fastify Backend URL
# Public variable accessible in browser (prefixed with NEXT_PUBLIC_)
NEXT_PUBLIC_FASTIFY_URL="http://localhost:3001"

# Service-to-Service Authentication (PRODUCTION ONLY)
# Shared secret for JWT token generation when calling Fastify API
# REQUIRED when using authenticated Fastify client (see lib/fastify-client.ts)
#
# Generate a secure secret with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# IMPORTANT: Use the SAME secret as in backend .env file
# SERVICE_SECRET="your-secure-random-secret-here-min-32-characters"

# Next.js Authentication Secret (for demo session cookies)
# Used by Next.js middleware and API routes for user authentication
# This is separate from SERVICE_SECRET (which is for Next.js → Fastify communication)
# NEXTAUTH_SECRET="your-nextauth-secret-for-user-sessions"

# Next.js Configuration
# NEXT_PUBLIC_APP_URL="http://localhost:3000"
